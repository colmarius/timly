{
  "name": "timly",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "patch-package": "^6.4.7"
  },
  "workspaces": [
    "packages/app",
    "packages/expo",
    "packages/next"
  ],
  "scripts": {
    "dev:expo": "yarn workspace @timly/expo dev",
    "dev:next": "yarn workspace @timly/next dev",
    "build": "cd packages/next && yarn build",
    "deploy": "vercel --prod --no-clipboard",
    "lint": "npx eslint --fix ./packages",
    "postinstall": "patch-package"
  },
  "resolutions": {
    "babel-preset-expo": "^8.5.1"
  }
}
