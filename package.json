{
  "name": "timly",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "patch-package": "^6.4.7"
  },
  "workspaces": [
    "packages/app",
    "packages/mobile",
    "packages/next"
  ],
  "scripts": {
    "mobile:dev": "yarn workspace @timly/mobile dev",
    "mobile:start": "yarn workspace @timly/mobile start:mobile",
    "dev:next": "yarn workspace @timly/next dev",
    "build": "cd packages/next && yarn build",
    "deploy": "vercel --prod --no-clipboard",
    "lint": "npx eslint --fix ./packages",
    "postinstall": "patch-package"
  },
  "resolutions": {
    "babel-preset-expo": "^8.5.1"
  },
  "dependencies": {}
}
